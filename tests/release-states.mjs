import {createRequire} from 'node:module';import assert from 'node:assert/strict';const require=createRequire(process.env.PLAYWRIGHT_PACKAGE),{chromium}=require('playwright');const b=await chromium.launch({channel:'msedge',headless:true}),c=await b.newContext({serviceWorkers:'block'}),p=await c.newPage(),base=process.env.TEST_URL||'http://127.0.0.1:8767/';
try{await p.goto(base);await p.locator('.library-row').first().waitFor();for(const [id,asset] of [['nativity','nativity.mxl'],['scripture-power','pdfs/scripture-power.pdf']]){const url=new URL('assets/'+asset,base).href;await p.route(url,r=>r.abort());await p.locator(`[data-song="${id}"] .song-entry`).click();await p.waitForFunction(()=>document.body.classList.contains('library-open')&&document.getElementById('library-message').textContent.includes('Unable'));assert.equal(await p.locator('#library-message').textContent(),'Unable to open this score. Try again online or choose another song.');await p.unroute(url);await p.locator(`[data-song="${id}"] .song-entry`).click();await p.waitForFunction(()=>prototype.ready&&!prototype.busy&&document.getElementById('score').getAttribute('aria-busy')==='false');await p.locator('#songs').click();assert.equal(await p.locator('#library-message').textContent(),'');}
await p.route('**/vendor/opensheetmusicdisplay.min.js',r=>r.fulfill({status:200,contentType:'text/javascript',body:''}));await p.reload();await p.locator('#library-message').filter({hasText:'Unable to start MusicTranspose'}).waitFor();assert(await p.locator('#library-message').isVisible());
const lum=h=>{const c=h.match(/\w\w/g).map(x=>parseInt(x,16)/255).map(x=>x<=.04045?x/12.92:((x+.055)/1.055)**2.4);return c[0]*.2126+c[1]*.7152+c[2]*.0722;};const contrast=(lum('e2e8ec')+.05)/(lum('536b7a')+.05);assert(contrast>=4.5);console.log('PASS concise asset/startup failures, visible startup message, successful retry clears stale error; header accent contrast '+contrast.toFixed(2)+':1');}finally{await b.close();}
